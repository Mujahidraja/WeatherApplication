name: assignment4
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ">=3.5.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
    http: ^0.13.0
  firebase_core: ^2.18.1
  firebase_auth: ^3.3.0
  cloud_firestore: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true